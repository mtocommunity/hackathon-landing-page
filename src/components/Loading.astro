---
/**
 * Loading Component - Wrapper Principal
 * Importa el componente desde la carpeta refactorizada
 */

// Import the new Loading structure
import LoadingComponent from './Loading/Loading.astro';
export type { LoadingProps } from './Loading/Loading.types';

// Pass through all props to the new component
const props = Astro.props;
---

<LoadingComponent {...props}>
  <slot />
</LoadingComponent>
