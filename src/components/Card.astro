---
/**
 * Card Component - Wrapper Principal
 * Importa el componente desde la carpeta refactorizada
 */

// Import the new Card structure
import CardComponent from './Card/Card.astro';
export type { CardProps } from './Card/Card.types';

// Pass through all props to the new component
const props = Astro.props;
---

<CardComponent {...props}>
  <slot />
</CardComponent>