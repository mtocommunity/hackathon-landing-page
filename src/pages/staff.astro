---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import Button from '../components/Button.astro';
import Card from '../components/Card.astro';

// Importar datos del staff desde el store
import { useStaff } from '../lib/store';

const staffMembers = useStaff().getAll();

const areas = [
  {
    name: 'Dirección',
    description: 'Liderazgo estratégico y visión del evento',
    icon: '🎯',
    color: 'var(--accent-teal)'
  },
  {
    name: 'Operaciones',
    description: 'Logística y coordinación del evento',
    icon: '⚙️',
    color: 'var(--accent-purple)'
  },
  {
    name: 'Tecnología',
    description: 'Infraestructura técnica y soporte',
    icon: '💻',
    color: 'var(--accent-pink)'
  },
  {
    name: 'Marketing',
    description: 'Comunicación y gestión de comunidad',
    icon: '📢',
    color: 'var(--accent-teal)'
  },
  {
    name: 'Mentoría',
    description: 'Guía técnica y apoyo a participantes',
    icon: '🎓',
    color: 'var(--accent-purple)'
  },
  {
    name: 'Diseño',
    description: 'Experiencia de usuario y diseño visual',
    icon: '🎨',
    color: 'var(--accent-pink)'
  }
];

const timeline = [
  {
    period: '6 meses antes',
    activities: [
      'Planificación estratégica del evento',
      'Definición de objetivos y métricas',
      'Búsqueda de patrocinadores',
      'Selección del jurado'
    ]
  },
  {
    period: '3 meses antes',
    activities: [
      'Desarrollo de la plataforma web',
      'Diseño de materiales promocionales',
      'Lanzamiento de la campaña de marketing',
      'Coordinación logística del venue'
    ]
  },
  {
    period: '1 mes antes',
    activities: [
      'Apertura de inscripciones',
      'Preparación de workshops y mentorías',
      'Finalización de detalles logísticos',
      'Coordinación con universidades'
    ]
  },
  {
    period: 'Durante el evento',
    activities: [
      'Recepción y registro de participantes',
      'Coordinación de mentorías',
      'Soporte técnico continuo',
      'Documentación y cobertura del evento'
    ]
  }
];
---

<Layout title="Equipo Organizador - MTO Hackathon 2024">
  <Header currentPath="/staff" />
  
  <main>
    <!-- Hero Section -->
    <section class="hero">
      <div class="container">
        <div class="hero__content text-center">
          <h1 class="hero__title">
            Nuestro <span class="text-gradient">Equipo</span>
          </h1>
          <p class="hero__description">
            Conoce a las personas apasionadas que hacen posible este increíble evento. 
            Un equipo multidisciplinario comprometido con la innovación y la tecnología.
          </p>
        </div>
      </div>
    </section>

    <!-- Areas Section -->
    <section class="areas">
      <div class="container">
        <div class="text-center">
          <h2 class="section-title">Áreas de Trabajo</h2>
          <p class="section-description">
            Nuestro equipo se organiza en diferentes áreas especializadas para garantizar el éxito del evento
          </p>
        </div>
        
        <div class="areas__grid">
          {areas.map((area) => (
            <Card variant="elevated" hoverable class="area-card">
              <div class="area__icon" style={`color: ${area.color}`}>
                {area.icon}
              </div>
              <h3 class="area__name">{area.name}</h3>
              <p class="area__description">{area.description}</p>
            </Card>
          ))}
        </div>
      </div>
    </section>

    <!-- Staff Section -->
    <section class="staff">
      <div class="container">
        <div class="text-center">
          <h2 class="section-title">Conoce al Equipo</h2>
          <p class="section-description">
            Profesionales dedicados que trabajan incansablemente para crear una experiencia única
          </p>
        </div>
        
        <div class="staff__grid">
          {staffMembers.map((member) => (
            <Card variant="elevated" hoverable class="staff-card">
              <div class="staff__header">
                <div class="staff__avatar">
                  <img 
                    src={member.imagen} 
                    alt={`${member.nombre} ${member.apellidos}`}
                    onerror="this.src='/images/placeholder-avatar.jpg'"
                  />
                </div>
                <div class="staff__info">
                  <h3 class="staff__name">{member.nombre} {member.apellidos}</h3>
                  <p class="staff__cargo">{member.cargo}</p>
                  <p class="staff__area">{member.area}</p>
                </div>
              </div>
              
              <p class="staff__bio">{member.bio}</p>
              
              <div class="staff__especialidades">
                <h4>Especialidades:</h4>
                <div class="especialidades-tags">
                  {member.especialidades.map((especialidad) => (
                    <span class="especialidad-tag">{especialidad}</span>
                  ))}
                </div>
              </div>
              
              <div class="staff__social">
                {member.redes.linkedin && (
                  <a href={member.redes.linkedin} class="social-link" aria-label="LinkedIn">
                    <i class="icon-linkedin"></i>
                  </a>
                )}
                {member.redes.twitter && (
                  <a href={member.redes.twitter} class="social-link" aria-label="Twitter">
                    <i class="icon-twitter"></i>
                  </a>
                )}
                {member.redes.github && (
                  <a href={member.redes.github} class="social-link" aria-label="GitHub">
                    <i class="icon-github"></i>
                  </a>
                )}
                {member.redes.instagram && (
                  <a href={member.redes.instagram} class="social-link" aria-label="Instagram">
                    <i class="icon-instagram"></i>
                  </a>
                )}
              </div>
            </Card>
          ))}
        </div>
      </div>
    </section>

    <!-- Process Section -->
    <section class="process">
      <div class="container">
        <div class="text-center">
          <h2 class="section-title">Nuestro Proceso</h2>
          <p class="section-description">
            Conoce cómo organizamos este evento épico desde los primeros pasos hasta el día del hackathon
          </p>
        </div>
        
        <div class="timeline">
          {timeline.map((phase, index) => (
            <Card variant="elevated" class="timeline-card">
              <div class="timeline__header">
                <div class="timeline__number">{index + 1}</div>
                <h3 class="timeline__period">{phase.period}</h3>
              </div>
              <ul class="timeline__activities">
                {phase.activities.map((activity) => (
                  <li>{activity}</li>
                ))}
              </ul>
            </Card>
          ))}
        </div>
      </div>
    </section>

    <!-- Values Section -->
    <section class="values">
      <div class="container">
        <div class="text-center">
          <h2 class="section-title">Nuestros Valores</h2>
          <p class="section-description">
            Los principios que guían nuestro trabajo y definen la cultura de MTO
          </p>
        </div>
        
        <div class="values__grid">
          <Card variant="elevated" hoverable class="value-card">
            <div class="value__icon">🚀</div>
            <h3 class="value__title">Innovación</h3>
            <p class="value__description">
              Buscamos constantemente nuevas formas de hacer las cosas mejor, 
              fomentando la creatividad y el pensamiento disruptivo.
            </p>
          </Card>
          
          <Card variant="elevated" hoverable class="value-card">
            <div class="value__icon">🤝</div>
            <h3 class="value__title">Colaboración</h3>
            <p class="value__description">
              Creemos en el poder del trabajo en equipo y la construcción 
              de puentes entre diferentes disciplinas y backgrounds.
            </p>
          </Card>
          
          <Card variant="elevated" hoverable class="value-card">
            <div class="value__icon">⭐</div>
            <h3 class="value__title">Excelencia</h3>
            <p class="value__description">
              Nos comprometemos a entregar la mejor experiencia posible, 
              cuidando cada detalle del evento.
            </p>
          </Card>
          
          <Card variant="elevated" hoverable class="value-card">
            <div class="value__icon">🌍</div>
            <h3 class="value__title">Impacto</h3>
            <p class="value__description">
              Aspiramos a generar un impacto positivo en la comunidad 
              tecnológica y en la sociedad en general.
            </p>
          </Card>
          
          <Card variant="elevated" hoverable class="value-card">
            <div class="value__icon">📚</div>
            <h3 class="value__title">Aprendizaje</h3>
            <p class="value__description">
              Promovemos el aprendizaje continuo y el intercambio de 
              conocimientos entre todos los participantes.
            </p>
          </Card>
          
          <Card variant="elevated" hoverable class="value-card">
            <div class="value__icon">🎯</div>
            <h3 class="value__title">Transparencia</h3>
            <p class="value__description">
              Mantenemos una comunicación abierta y honesta en todos 
              nuestros procesos y decisiones.
            </p>
          </Card>
        </div>
      </div>
    </section>

    <!-- Contact Section -->
    <section class="contact">
      <div class="container">
        <Card variant="elevated" size="lg" class="contact-card text-center">
          <h2 class="contact__title">¿Quieres Unirte al Equipo?</h2>
          <p class="contact__description">
            Siempre estamos buscando personas talentosas y apasionadas por la tecnología 
            que quieran formar parte de la familia MTO.
          </p>
          <div class="contact__actions">
            <Button variant="primary" size="lg" href="mailto:team@mto.pe">
              Envíanos tu CV
            </Button>
            <Button variant="ghost" size="lg" href="/inscripcion">
              Participar como Concursante
            </Button>
          </div>
        </Card>
      </div>
    </section>
  </main>
  
  <Footer />
</Layout>

<style>
  .hero {
    padding: calc(70px + var(--spacing-3xl)) 0 var(--spacing-3xl);
  }

  .hero__title {
    font-size: var(--font-size-5xl);
    font-weight: 900;
    margin-bottom: var(--spacing-lg);
  }

  .hero__description {
    font-size: var(--font-size-xl);
    color: var(--text-secondary);
    max-width: 700px;
    margin: 0 auto;
  }

  .areas, .staff, .process, .values, .contact {
    padding: var(--spacing-3xl) 0;
  }

  .section-title {
    font-size: var(--font-size-4xl);
    font-weight: 800;
    margin-bottom: var(--spacing-lg);
    background: var(--gradient-button);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .section-description {
    font-size: var(--font-size-lg);
    color: var(--text-secondary);
    max-width: 600px;
    margin: 0 auto var(--spacing-3xl);
  }

  .areas__grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--spacing-xl);
  }

  .area-card {
    text-align: center;
    position: relative;
  }

  .area__icon {
    font-size: var(--font-size-4xl);
    margin-bottom: var(--spacing-lg);
  }

  .area__name {
    font-size: var(--font-size-xl);
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: var(--spacing-sm);
  }

  .area__description {
    color: var(--text-secondary);
    line-height: 1.6;
  }

  .staff__grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: var(--spacing-xl);
  }

  .staff-card {
    position: relative;
  }

  .staff__header {
    display: flex;
    align-items: flex-start;
    gap: var(--spacing-lg);
    margin-bottom: var(--spacing-lg);
  }

  .staff__avatar {
    width: 70px;
    height: 70px;
    border-radius: 50%;
    overflow: hidden;
    box-shadow: var(--shadow-button);
    flex-shrink: 0;
  }

  .staff__avatar img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    background: var(--gradient-card);
  }

  .staff__info {
    flex: 1;
  }

  .staff__name {
    font-size: var(--font-size-lg);
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: var(--spacing-xs);
  }

  .staff__cargo {
    font-size: var(--font-size-base);
    font-weight: 600;
    color: var(--accent-teal);
    margin-bottom: var(--spacing-xs);
  }

  .staff__area {
    font-size: var(--font-size-sm);
    color: var(--text-secondary);
  }

  .staff__bio {
    color: var(--text-secondary);
    line-height: 1.6;
    margin-bottom: var(--spacing-lg);
  }

  .staff__especialidades {
    margin-bottom: var(--spacing-lg);
  }

  .staff__especialidades h4 {
    color: var(--text-primary);
    font-weight: 600;
    margin-bottom: var(--spacing-sm);
    font-size: var(--font-size-base);
  }

  .especialidades-tags {
    display: flex;
    flex-wrap: wrap;
    gap: var(--spacing-xs);
  }

  .especialidad-tag {
    background: var(--gradient-button);
    color: var(--text-primary);
    padding: var(--spacing-xs) var(--spacing-sm);
    border-radius: var(--radius-sm);
    font-size: var(--font-size-xs);
    font-weight: 500;
  }

  .staff__social {
    display: flex;
    gap: var(--spacing-sm);
  }

  .social-link {
    width: 36px;
    height: 36px;
    background: var(--gradient-card);
    border-radius: var(--radius-sm);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--text-secondary);
    text-decoration: none;
    transition: var(--transition-fast);
    box-shadow: var(--shadow-button);
  }

  .social-link:hover {
    color: var(--accent-teal);
    transform: translateY(-2px);
  }

  .timeline {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: var(--spacing-xl);
  }

  .timeline-card {
    position: relative;
  }

  .timeline__header {
    display: flex;
    align-items: center;
    gap: var(--spacing-md);
    margin-bottom: var(--spacing-lg);
  }

  .timeline__number {
    width: 50px;
    height: 50px;
    background: var(--gradient-button);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: var(--font-size-lg);
    font-weight: 800;
    color: var(--text-primary);
    box-shadow: var(--shadow-button);
    flex-shrink: 0;
  }

  .timeline__period {
    font-size: var(--font-size-lg);
    font-weight: 700;
    color: var(--text-primary);
  }

  .timeline__activities {
    list-style: none;
    padding: 0;
  }

  .timeline__activities li {
    color: var(--text-secondary);
    padding: var(--spacing-xs) 0;
    position: relative;
    padding-left: var(--spacing-lg);
  }

  .timeline__activities li::before {
    content: "✓";
    position: absolute;
    left: 0;
    color: var(--accent-teal);
    font-weight: bold;
  }

  .values__grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: var(--spacing-xl);
  }

  .value-card {
    text-align: center;
  }

  .value__icon {
    font-size: var(--font-size-4xl);
    margin-bottom: var(--spacing-lg);
  }

  .value__title {
    font-size: var(--font-size-xl);
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: var(--spacing-md);
  }

  .value__description {
    color: var(--text-secondary);
    line-height: 1.6;
  }

  .contact__title {
    font-size: var(--font-size-4xl);
    font-weight: 800;
    margin-bottom: var(--spacing-lg);
    background: var(--gradient-button);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .contact__description {
    font-size: var(--font-size-lg);
    color: var(--text-secondary);
    margin-bottom: var(--spacing-2xl);
  }

  .contact__actions {
    display: flex;
    gap: var(--spacing-lg);
    justify-content: center;
  }

  /* Iconos de redes sociales */
  .icon-linkedin::before { content: "💼"; }
  .icon-twitter::before { content: "🐦"; }
  .icon-github::before { content: "🐙"; }
  .icon-instagram::before { content: "📷"; }

  /* Responsive */
  @media (max-width: 768px) {
    .areas__grid {
      grid-template-columns: 1fr;
    }

    .staff__grid {
      grid-template-columns: 1fr;
    }

    .staff__header {
      flex-direction: column;
      text-align: center;
    }

    .staff__avatar {
      margin: 0 auto;
    }

    .timeline {
      grid-template-columns: 1fr;
    }

    .values__grid {
      grid-template-columns: 1fr;
    }

    .contact__actions {
      flex-direction: column;
      align-items: center;
    }
  }

  @media (max-width: 480px) {
    .staff__grid {
      grid-template-columns: 1fr;
    }

    .timeline__header {
      flex-direction: column;
      text-align: center;
    }

    .timeline__number {
      margin-bottom: var(--spacing-sm);
    }
  }
</style>
